trait Int['a] {
    default(int)
}

impl Int[int] {}

trait Add['a, 'b, 'c] {
    fn +(a: 'a, b: 'b) -> 'c
}

impl Add[int, int, int] {
    @inline
    fn +(a: int, b: int) -> int => asm { $int_add a b }
}

fn main() {
    i = 1
    j = 2
    k = i + j
}
