trait Int['a] {}

trait Lt['a, 'b] {
    fn <(a: 'a, b: 'b) -> bool
}

trait Neg['a, 'b] {
    fn -(a: 'a) -> 'b
}

fn abs(a: 'a) -> 'a where Int['a], Lt['a, 'a], Neg['a, 'a] {
    if a < 0 {
        -a
    } else {
        a
    }
}
