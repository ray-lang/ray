//![no-core]

struct Str {}

trait ToStr['a] {
    fn to_str(self: 'a) -> Str
}

struct A{}

impl ToStr[A] {
    fn to_str(self: A) -> Str {
        Str{}
    }
}

fn main() {
    a = A{}
    s = a.to_str()
}
