trait Int['a] {}

trait Lt['a, 'b] {
    fn <(a: 'a, b: 'b) -> bool
}

trait Mul['a, 'b, 'c] {
    fn *(a: 'a, b: 'b) -> 'c
}

trait Neg['a, 'b] {
    fn -(a: 'a) -> 'b
}

fn abs(a: 'a) -> 'a where Lt['a, 'a], Mul['a, 'a, 'a] {
    if a < 0 {
        a * -1
    } else {
        a
    }
}

