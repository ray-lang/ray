//![no-core]

struct list['a] {
    values: rawptr['a]
    len: uint
    capacity: uint
}

trait Index['a, 'el, 'idx] {
    fn get(self: *'a, idx: 'idx) -> 'el?
}

impl Index[list['a], 'el, uint] {
    fn get(self: *list['a], idx: uint) -> 'el? {
        ptr = self.values
        l = self.len
        nil
    }
}
