struct list['a] {
    values: *'a
    len: uint
    capacity: uint
}

impl Index[list['a], 'el, uint] {
    fn get(self: 'a, idx: uint) -> 'el? {
        nil
    }

    fn set(self: 'a, idx: uint, el: 'el) -> 'el? {
        nil
    }
}
