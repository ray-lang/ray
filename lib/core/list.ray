struct list['a] {
    values: rawptr['a]
    len: uint
    capacity: uint
}

impl Index[list['a], 'el, uint] {
    fn get(self: *list['a], idx: uint) -> 'el? {
        if idx < self.len {
            return nil
        }

        ptr = self.values
        nil
    }

    fn set(self: *'a, idx: uint, el: 'el) -> 'el? {
        nil
    }
}
