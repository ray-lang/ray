trait ToStr['a] {
    fn to_str(self: 'a) -> string
}

impl ToStr[int] {
    fn to_str(self: int) -> string {
        "0"
        // s = ""
        // i = self
        // while i != 0 {
        //     s = ((('0' as u8) + (i%10 as u8)) as char) + s

        // }
    }
}
